apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: crud-mean
  description: Aplicación Fullstack (MEAN Stack) para operaciones CRUD básicas.
  # Etiquetas para facilitar la búsqueda en el catálogo
  tags:
    - javascript
    - mongodb
    - express
    - angular
    - nodejs
  annotations:
    # Si quieres que se vea la documentación, puedes apuntar a tu carpeta de docs
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: experimental
  owner: user:guest # O el nombre de tu equipo/usuario
  system: mean-apps # Opcional: para agruparlo con otros proyectos relacionados
